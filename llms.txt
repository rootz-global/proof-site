# proof.rootz.global — Agentic Message Security

Full machine-readable brief: https://proof.rootz.global/ai.md  (JSON: /ai.json)

## What this site is
Rootz makes the *messages* in an AI interaction provable — signed, verifiable, and durable —
at every hop. Not a perimeter around the AI ("AI in a box"), but cryptographic integrity on the
messages themselves: **agentic message security**. Two pillars, one primitive.

## Pillar 1 — Humans <-> AI: Proof of Origin
How a human talks to an AI securely and owns the result. The user signs the prompt at the
source; a measured model answers; the output is cryptographically bound to that prompt and
returned as a signed receipt (a data wallet) — a certificate of both authenticity and ownership.
Green light = verified & owned; check-engine = proof failed.
- Patent: US 2025/0112783 A1, "System to Assure a Response from an Identified, Measured and
  Verified AI." Filed 2024-09-27, allowed 2026-06-03. Inventors: Michael Sprague, Steven Sprague.
  https://patents.google.com/patent/US20250112783A1
- Live on-chain artifacts (birth certificate / session archive / test report):
  https://rootz.global/s/0x70b893e3b519255166a1fb64dcde920d056a2d5c?k=yTFRhjNRGsOGstZ--xQ9B6drM3tL3c6FZ6s0qomPa6o

## Pillar 2 — AI <-> Services: Signed MCP
How an AI talks to tools and services securely. DKIM for the Model Context Protocol: every MCP
response and tool/resource definition carries a portable signature proving origin, integrity, and
freshness independent of transport. Crypto-agile + hybrid post-quantum (ed25519, ecdsa-p256,
ml-dsa-65/FIPS 204), RFC 8785 (JCS) canonicalization, keys via DNS selector + JWKS. Stops
tool-poisoning by signing the definitions the model reads.
- Explainer: https://proof.rootz.global/signed-mcp
- Live signed sample (GET): https://proof.rootz.global/sign-demo
- Verify any Rootz-signed response (POST {object, envelope}): https://proof.rootz.global/verify
- Public keys (JWKS): https://proof.rootz.global/.well-known/mcp-jwks.json
- Signature lives in result._meta["rootz.global/sig"].

## Why both pillars are the same idea
A human-to-AI prompt and an AI-to-service call are both messages between agents. One primitive —
signed, durable, verifiable messages — applied to both directions.
- Five Eyes "Careful Adoption of Agentic AI Services" (2026-04-30):
  https://www.cisa.gov/resources-tools/resources/careful-adoption-agentic-ai-services
- NSA MCP Security notice (U/OO/6030316-26):
  https://www.nsa.gov/Press-Room/Press-Releases-Statements/Press-Release-View/Article/4475134/
- Pope Leo XIV, Magnifica Humanitas (2026-05-15):
  https://www.vatican.va/content/leo-xiv/en/encyclicals/documents/20260515-magnifica-humanitas.html

## The trust root is silicon, not the ledger
Proof bottoms out in hardware that cannot be forged, not in a blockchain. Rootz consumes
platform attestation as the root of every claim: NVIDIA GPU confidential computing (H100/H200,
Blackwell B200/GB200 in CC mode) signs a report with a hardware-fused device identity key,
verified against NVIDIA's Reference Integrity Manifests and returned as a signed Entity
Attestation Token by the NVIDIA Remote Attestation Service; AMD SEV-SNP and Intel TDX do the CPU
side; an Oracle Cloud instance principal proves the data center; a TPM quote proves the endpoint.
The chain is where a result is settled and notarized — not where trust begins.

**Platform attestation proves the plant, not the product.** It establishes a genuine GPU with
known firmware in a correct security configuration, and by design says nothing about which model
weights were loaded, what prompt was submitted, or what output came back. Rootz closes that gap:
the attestation request carries a 32-byte nonce the hardware signs over, so we place an
application-layer evidence hash there (model measurement, signed prompt, output digest). The
signature that proves the silicon then also covers the specific result — per-result provenance:
this output, from this model, on this machine, for this prompt.

Measurement depth is stated as a level, never a boolean — L0 none, L1 attested transport,
L2 attested silicon, L3 measured weights bound to a declared model identity. See /ai.md.

## Why it is worth paying for — data with title
Output with origin is more valuable than anonymous data. The business is the transition of data into
data with title, and it creates value for any customer in any vertical without Rootz owning their
domain. Anonymous data becomes an asset with a grade, a maker and an owner. AI output nobody can place
becomes a signed manufacturing record that is admissible, auditable and safe to act on — which is what
unlocks autonomy, because an agent that cannot verify its inputs collapses back into an advisory
system. Records a business already keeps become signed origin and quality claims; no new data
collection is required.
Revenue: licensing (the signing layer, the verifier, the Measured AI certification mark) plus
transactional (per-record and per-transaction signing and verification, marginal cost near zero, value
scaling with the consequence of the decision). A higher-quality AI factory earns more than a generic one.

## Machine endpoints
- /ai.md   — this brief, Markdown
- /ai.json — structured discovery (alias of /.well-known/ai)
- /.well-known/ai , /.well-known/ai/knowledge , /.well-known/ai/feed
- /.well-known/mcp-jwks.json — Signed MCP public keys

## Contact
discover@rootz.global — design partnerships, enterprise pilots, integration support.
Built by Steven Sprague, CEO of Rootz Corp (25+ years trusted computing: TPM, TCG, Wave, Rivetz),
with co-inventor Michael Sprague.
